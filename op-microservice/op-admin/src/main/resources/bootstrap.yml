spring:
  application:
    name: op-admin
  cloud:
    nacos:
      config:
        server-addr: host.docker.internal:8848
        file-extension: yaml
      discovery:
        server-addr: host.docker.internal:8848